---
const isProduction = import.meta.env.MODE === "production";
const FathomSiteID = "RFDHXHDZ";
---

{
  isProduction && (
    <script
      is:inline
      src="https://cdn.usefathom.com/script.js"
      data-site={FathomSiteID}
      defer
    />
  )
}
