import { useStore } from "../../store";

describe("Returns undefined and does not auto-answer unsupported inputs", () => {
  test.todo("If the node is not a supported Input type");

  test.todo("If the node does not set a `fn`");

  test.todo("If we've never seen another node with this `fn` before");

  test.todo(
    "If we've never seen a node of this same type with this `fn` before",
  );
});

describe("AddressInputs", () => {
  test.todo(
    "Outputs the expected breadcrumb format for an auto-answerable node",
  );
});

describe("ContactInputs", () => {
  test.todo(
    "Outputs the expected breadcrumb format for an auto-answerable node",
  );
});

describe("DateInput", () => {
  test.todo(
    "Outputs the expected breadcrumb format for an auto-answerable node",
  );
});

describe("NumberInput", () => {
  test.todo(
    "Outputs the expected breadcrumb format for an auto-answerable node",
  );
});

describe("TextInput", () => {
  test.todo(
    "Outputs the expected breadcrumb format for an auto-answerable node",
  );
});
