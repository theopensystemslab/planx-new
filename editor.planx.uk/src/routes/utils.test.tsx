import { isSaveReturnFlow, setPath } from "./utils";

describe("isSaveReturnFlow helper function", () => {
  it.todo("correctly identifies flows with a 'Send' node");
  it.todo("correctly identifies flows without a 'Send' node");
});

describe("setPath helper function", () => {
  it.todo("correctly sets the path for a 'Save/Return' user journey");
  it.todo("correctly sets the path for a 'Single session' user journey");
  it.todo("correctly sets the path for a 'Resume' user journey");
});
