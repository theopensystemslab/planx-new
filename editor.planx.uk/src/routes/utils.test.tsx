import { isSaveReturnFlow } from "./utils";

describe("isSaveReturnFlow helper function", () => {
  it.todo("correctly identifies flows with a 'Send' node");
  it.todo("correctly identifies flows without a 'Send' node");
});
