# Single source of truth for Prettier ignores across the monorepo
# lint-staged runs prettier from the repo root and picks this up automatically
# App lint scripts that run prettier from their own dir reference it 
# explicitly via the --ignore-path flag

pnpm-lock.yaml
**/routeTree.gen.ts

apps/api.planx.uk/dist
apps/api.planx.uk/coverage

apps/localplanning.services/public/mockServiceWorker.js
