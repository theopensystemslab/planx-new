{
  "default": {
    "loader": ["ts-node/esm"],
    "import": ["src/env.ts", "src/**/*.ts"],
    "paths": ["src/**/*.feature"],
    "format": ["summary"],
    "formatOptions": {
      "snippetInterface": "async-await"
    }
  }
}
