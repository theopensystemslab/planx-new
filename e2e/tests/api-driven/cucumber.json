{
  "default": {
    "requireModule": ["ts-node/register"],
    "require": ["src/env.ts", "src/**/*.ts"],
    "paths": ["src/**/*.feature"],
    "format": ["summary"],
    "publishQuiet": true,
    "formatOptions": {
      "snippetInterface": "async-await"
    }
  }
}
