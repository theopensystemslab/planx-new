table:
  name: service_charges
  schema: public
select_permissions:
  - role: platformAdmin
    permission:
      columns:
        - service_charge_amount
        - paid_at_month
        - paid_at_quarter
        - paid_at_year
        - flow_name
        - flow_slug
        - paid_at_month_text
        - payment_id
        - team_slug
        - paid_at
        - session_id
      filter: {}
    comment: ""
