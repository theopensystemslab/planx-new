{
  "scripts": {
    "up": "docker compose -f ./docker-compose.yml -f ./docker-compose.local.yml up -d",
    "down": "docker compose -f ./docker-compose.yml -f ./docker-compose.local.yml down --remove-orphans -v"
  }
}
