{
  "name": "@planx/infrastructure-application",
  "prettier": "@planx/prettier-config",
  "scripts": {
    "check": "pnpm typecheck && pnpm lint",
    "typecheck": "tsc --noEmit",
    "lint": "eslint '**/*.{js,ts}' && prettier -c .",
    "lint:fix": "eslint --fix '**/*.{js,ts}' && prettier -w ."
  },
  "dependencies": {
    "@nodelib/fs.walk": "^3.0.1",
    "@planx/infrastructure-common": "workspace:*",
    "@pulumi/aws": "catalog:infrastructure",
    "@pulumi/awsx": "catalog:infrastructure",
    "@pulumi/cloudflare": "catalog:infrastructure",
    "@pulumi/postgresql": "catalog:infrastructure",
    "@pulumi/pulumi": "catalog:infrastructure",
    "tldjs": "^2.3.2"
  },
  "devDependencies": {
    "@planx/eslint-config": "workspace:*",
    "@planx/prettier-config": "workspace:*",
    "@planx/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/tldjs": "^2.3.4",
    "eslint": "catalog:",
    "globals": "catalog:",
    "mime": "^4.1.0",
    "prettier": "catalog:",
    "typescript": "catalog:typescript"
  },
  "private": true
}
