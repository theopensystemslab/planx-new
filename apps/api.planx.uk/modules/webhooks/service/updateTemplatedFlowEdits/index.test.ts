describe("Update templated flow edits controller", () => {
  it.todo("fails without correct authentication");

  it.todo("returns a 200 but 'skips' logic if this is not a templated flow");

  it.todo(
    "returns the flowId, delta and mutation response on success for a templated flow",
  );
});

describe("Update templated flow edits logic", () => {
  it.todo("returns undefined if there is no source template data");

  it.todo("returns undefined if there is no delta");

  it.todo("returns the flowId, delta and mutation response");
});
