describe("transformDeltaToTemplatedFlowEditsData", () => {
  it.todo("captures a changed node data property");

  it.todo("captures a new node data property");

  it.todo("captures a deleted (aka nullified) node data property");

  it.todo(
    "captures a new option (aka 'edge') on an existing node and the new option node itself",
  );
});
