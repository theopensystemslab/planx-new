describe("isApplicationTypeSupported", () => {
  test.todo("returns true for statutory application types");
  test.todo("return true for pre-applications");
  test.todo("returns false for discretionary types");
  test.todo("returns false if passport does not have application.type key");
});